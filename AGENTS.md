# Codex作業ルール

このリポジトリは、個人制作プロジェクト「onile-shopping」です。
Codexやサブエージェントが作業するときは、このファイルのルールを必ず確認してください。

## 基本情報

- GitHubリポジトリ: `Aliingar/onile-shopping`
- 作業場所: `G:\マイドライブ\Backup\School PC backup file\kozin`
- ローカル想定: `G:\マイドライブ\Backup\School PC backup file\kozin\onile-shopping`
- このプロジェクトは個人制作なので、初心者にも分かる日本語で説明してください。

## GitとPRのルール

- `main` ブランチで直接作業しないでください。
- 必ず作業用ブランチを作ってから変更してください。
- 変更は作業ブランチからPull Requestを作って確認します。
- `main` へ直接commit、push、mergeしないでください。
- 変更前に現在の構成と `git status` を確認してください。
- 変更後は、何を確認したかを日本語で報告してください。

## 秘密情報のルール

- `.env`、APIキー、DBパスワード、Aiven接続情報、トークン、GitHub Secretsを開かないでください。
- 秘密情報を画面やログに表示しないでください。
- APIキーやDBパスワードをコードへ直接書かないでください。

## DB運用ルール

- このプロジェクトのDBはMySQLです。
- MySQL DBはAiven Consoleで作る前提です。
- DBへ接続するときは、人間がDBeaverを使って接続する前提です。
- テーブル作成や確認は、DBeaver上でSQLを実行して行う前提です。
- Codexは実DBへ勝手に接続しないでください。
- CodexはAiven Console、DBeaver、実DBを勝手に操作しないでください。
- DB削除、テーブル削除、データ削除、破壊的なSQLは必ず人間に確認してください。
- `DROP`、`TRUNCATE`、`DELETE`、破壊的な `ALTER` は、許可なく実行しないでください。

## サブエージェントの使い分け

作業範囲を分けるため、次の4種類のサブエージェントを使います。

- `frontend`: HTML、CSS、JavaScript、画面、フォーム、ボタン、API接続、表示処理を担当します。
- `backend`: Python/Flask、APIルート、バリデーション、エラー処理、MySQL接続処理、JSON、APIテストを担当します。
- `database`: MySQL、テーブル設計、SQL案、Aiven注意点、DBeaver確認手順、主キー、外部キー、初期データ案を担当します。
- `frontend_design`: 画面レイアウト、色、余白、ボタン、カード、スマホ対応、使いやすさを担当します。

## サブエージェント自動選択ルール

Codexのサブエージェントは、完全自動で必ず起動する保証はありません。
そのため、メインエージェントは作業前に必ず内容を分類し、必要な場合は自分から該当サブエージェントを明示的に起動してください。

分類ルール:

- HTML、CSS、JavaScript、画面表示、フォーム、ボタン、API接続、ブラウザ確認が中心なら `frontend` サブエージェントを起動する
- Python/Flask、API、バリデーション、エラー処理、JSON、MySQL接続処理が中心なら `backend` サブエージェントを起動する
- MySQL、テーブル設計、SQL案、Aiven、DBeaver、主キー、外部キーが中心なら `database` サブエージェントを起動する
- レイアウト、色、余白、ボタンの見た目、カード、スマホ対応、使いやすさが中心なら `frontend_design` サブエージェントを起動する
- 複数分野にまたがる場合は、必要なサブエージェントを複数起動し、結果をメインエージェントがまとめる
- 迷った場合は、まず調査だけを行うサブエージェントを起動し、実装へ進む前に報告する
- ルール更新やドキュメントだけの作業は、サブエージェントを使わなくてもよいが、その理由を報告する

確実に使わせたい場合は、ユーザーも「○○サブエージェントを明示的に起動して」と書いてください。

## 作業前報告ルール

Codexは作業前に、必ず次の内容を表示してください。

- サブエージェント判定: 使用する / 使用しない
- 使用するサブエージェント: frontend / backend / database / frontend_design / 複数 / なし
- 選んだ理由
- 今回の作業範囲: ○○
- 変更してよいファイル: ○○
- 変更してはいけないファイル: ○○
- 危険操作をしないことの確認

## メインエージェントの禁止事項

- サブエージェントを使うべき作業なのに、理由なくメインエージェントだけで実装しない
- フロント、バックエンド、DB、デザインを一気に大きく変更しない
- DB削除、秘密情報、実DB接続、Aiven/DBeaver操作は勝手にしない
- サブエージェントの結果を確認せずに実装へ進まない

## hooksの役割

hooksはサブエージェントを起動させるための仕組みではありません。
hooksは、秘密情報を表示しそうな操作や危険コマンドを検査するために使います。
サブエージェント選択はAGENTS.mdのルールに従います。
確実に使わせたい場合は、毎回のプロンプトにも「○○サブエージェントを明示的に起動して」と書いてください。

## フロントエンドデザイン作業の順番

フロントエンドのデザイン作業では、いきなりHTML、CSS、JavaScriptを変更しないでください。
次の順番を守ります。

1. `@creative-production` で0→1のデザインコンセプトを作る
2. `@product-design` で1→100の具体的なUI仕様にする
3. その後、必要な画面だけを小さく実装する
4. デザイン案なしでいきなりHTML、CSS、JavaScriptを変更しない
5. 実装するときは、変更ファイルと確認方法を報告する
6. API仕様やDB仕様を勝手に変えない

`@creative-production` では、アプリの雰囲気、方向性、画面のラフ案、ユーザーに与える印象、デザインコンセプトを考えます。
`@product-design` では、画面レイアウト、UI部品、色、余白、ボタン、カード、スマホ対応、使いやすさ、画面遷移、実装しやすいUI仕様まで具体化します。
HTML、CSS、JavaScriptへの反映は、デザイン案が固まったあとに別作業として小さなPRで行います。

## 最後の報告

作業が終わったら、初心者にも分かる日本語で次の内容を報告してください。

- 実施した内容
- 作成または変更したファイル
- 確認したコマンド
- 変更後の確認方法
- 注意点や、次に人間が確認すること
